import * as dbConstants from 'db.constants';

export enum IndicatorTypesEnum {
    ROLE = dbConstants.indicatorTypes.ROLE,
    STATUS = dbConstants.indicatorTypes.STATUS,
}

export enum IndicatorTypeNamesEnum {
    ROLE = 'Role',
    STATUS = 'Status',
}